const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Insurance Protocol Tests", function () {
    it("Should handle claims correctly", async function () {
        // Deploy your contract and write tests here
    });
});