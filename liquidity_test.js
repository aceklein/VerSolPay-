const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Liquidity Aggregator Tests", function () {
    it("Should return the correct liquidity", async function () {
        // Deploy your contract and write tests here
    });
});